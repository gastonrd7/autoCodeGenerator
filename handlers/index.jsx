const {campaign} = require('./campaign.jsx');
const {advertisement} = require('./advertisement.jsx');
const {company} = require('./company.jsx');
const {post} = require('./post.jsx');
const {insight} = require('./insight.jsx');

module.exports = { 
    campaign,
    advertisement,
    company,
    post,
    insight
};